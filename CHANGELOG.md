# Changelog — Perma Design Kit

Abgeschlossene Roadmap-Punkte, chronologisch nach Abarbeitung innerhalb ihres ursprünglichen Roadmap-Abschnitts. Offene Punkte stehen weiterhin in `ROADMAP.md`.

## Kurzfristig
- [x] **sanduhr fehlt bei erstellung von baumscheiben und pdf** — zwei Lücken gefunden: (1) das Kartenraster selbst zeigte während des Renderns (SVG-Fetch + Pro-Pflanze-DOM-Arbeit, spürbar bei Baumscheibe) keinerlei Ladezustand — neue `loadingPlaceholderHtml()` in `html.ts`, in `index.astro` und `cards.astro` vor dem `Promise.all` eingeblendet (nur für Baumscheibe, Poly/Streifen sind quasi instant, kein unnötiges Flackern). (2) der Bulk-PDF-Button (`btn-bulk-pdf`, Mehrfachauswahl in der Listenansicht) hatte überhaupt keinen `withButtonSpinner()` und ignorierte zudem `cardViewMode` — exportierte immer als Pflanzenkarte, auch wenn Baumscheibe gewählt war. Beides behoben, per Playwright verifiziert (Spinner synchron vor dem Await sichtbar, bestätigt an einem kalten Erst-Rendering).
- [x] **Breite in Tabelle als Spalte** — neue sortierbare Spalte „B" direkt neben „H" (Höhe) in der Desktop-Tabelle, analog implementiert (`sortField` um `'widthM'` erweitert, gleiches Sortier-/Spalten-Muster).
- [x] **Daten nachladen -> Menü offen halten und Stand anzeigen** — Bug: das „Daten"-Menü (`<details>`) schloss bei jedem Button-Klick sofort, auch bei „Lade alle fehlenden Daten" — dadurch war der Fortschrittstext („`1/N…`" im Button) nie sichtbar. Fix: `btn-enrich-all` von der Auto-Schließen-Logik ausgenommen; alle anderen Menüpunkte schließen weiterhin sofort. Verifiziert: Menü bleibt offen, Fortschritt sichtbar.
- [x] **Fehlende Pflanzen, die nicht von PFAF übernommen werden (Cynara cardunculus, Rubus phoenicolasius)** — echter Bug in `plant-search.ts`s `looksLikePlant()`, nicht im PFAF-Proxy (der lieferte für beide Arten von Anfang an korrekte Daten, direkt gegen `/api/plant-proxy` verifiziert). Der Nicht-Pflanzen-Ausschlussfilter prüfte Wikidata-Beschreibungen per `.includes('ant')` (u.a. für die Ameise) — trifft aber als Teilstring auch auf das Wort „pl**ant**" selbst, Wikidatas generische Fallback-Beschreibung „species of plant" für tausende Nebenarten ohne eigenen Beschreibungstext. Dadurch wurde die korrekte Basisart „Cynara cardunculus" (Wikidata-Beschreibung exakt „species of plant") aus den Suchergebnissen gefiltert, während ihre Unterarten/Sorten (reichhaltigere, nicht-generische Beschreibungen wie „cultivar of…") durchrutschten — bestätigt per direktem Netzwerk-Intercept der Wikidata-Antwort, nicht geraten. Gleiche Substring-Falle betraf vermutlich weitere Stichwörter (`'wurm'` in „Wurmfarn", `'vogel'` in „Vogelbeere"). Fix: Wortgrenzen-Regex (`\bwort s?\b`) statt `.includes()` — lässt Plural zu (`ants`), ohne „plant(s)" erneut zu treffen. Live verifiziert: „Cynara cardunculus" erscheint jetzt als erstes Ergebnis (vorher 5 von 12 Wikidata-Treffern gezeigt, jetzt alle 12), kompletter Such-→-Hinzufügen-→PFAF-Anreicherungs-Flow für „Wilde Artischocke" erfolgreich getestet.
- [x] **Sonne und Wasser fehlen in Tabelle -> als Spalten einfügen inkl. Filterung** — zwei neue Dot-Spalten „Sonne"/„Wasser" zwischen „Funktionen" und „Blüte/Frucht" in der Desktop-Tabelle (analog zu `USAGE_BADGES`/`FUNCTION_BADGES`: neue `SUN_BADGES`/`WATER_BADGES`-Tupel-Arrays, Legende erweitert), plus dieselben Dots in der mobilen Kartenansicht. Die alten rohen `.filter-chip`-Buttons für Sonne/Wasser (Teil des ursprünglichen, kombinierten `activeFilters`-Sets samt `persistFilters()`) wurden dabei komplett durch das etablierte Dropdown-Filter-Muster ersetzt (`<details>` mit Mehrfachauswahl-Checkboxen, `sunFilter`/`waterFilter` als eigene `Set`s über `loadSet`/`saveSet`, ODER-innerhalb/UND-zwischen-Spalten-Semantik wie bei Nutzung/Funktionen) — der alte, zuletzt nur noch für Sonne/Wasser genutzte `activeFilters`-Mechanismus wurde vollständig entfernt (keine verbleibenden Referenzen, per grep bestätigt), nicht nur ergänzt. Migrations-IIFE liest den alten `localStorage`-Schlüssel `plant-filters` einmalig aus, überträgt alle vier Auswahl-Gruppen (inkl. Sonne/Wasser) in die neuen Sets und löscht danach den alten Schlüssel. Per Playwright verifiziert: neue Tabellen-Header „Sonne"/„Wasser", Dots in Desktop-Tabelle und Mobile-Karte, Filter-Dropdowns filtern korrekt (z.B. „Volle Sonne" blendet Schatten-Pflanzen aus), „Zurücksetzen ×" setzt beide neuen Filter zurück, und die Migration eines simulierten alten `plant-filters`-Eintrags landet korrekt in den neuen Sets samt vorausgewählter Checkbox.
- [x] **Bei manchen Pflanzen fehlt der deutsche Name (z.B. Tagetes patula) — wie ergänzen?** — kein Fehlen im Sinne eines leeren Feldes, sondern ein stiller Wikidata-Eigenheit-Bug: Wikidata setzt bei Taxa ohne echten deutschen Trivialnamen das `labels.de`-Feld häufig auf den wissenschaftlichen Namen selbst (z.B. `labels.de` = „Tagetes patula", identisch zum lateinischen Namen) statt es leer zu lassen — der bisherige Code (`plant-search.ts`) prüfte nur `if (labelDe) …`, ein nicht-leerer, aber inhaltlich wertloser String zählte also fälschlich als „gefunden". Dadurch erschien in der App der lateinische Name doppelt (als `latinName` und `commonName`), was wie ein fehlender deutscher Name wirkt. Live gegen die Wikidata-API verifiziert: Wikidata hat für Trivialnamen ein eigenes, von `labels` unabhängiges Property `P1843` („taxon common name", sprachgetaggt) — für Cynara cardunculus liefert `labels.de` echt „Wilde Artischocke" (kein Bug dort), für Tagetes patula ist weder `labels.de` noch `P1843[de]` ein echter deutscher Name vorhanden, wohl aber `P1843[en]` = „French marigold". Fix: neue `bestCommonName()`-Hilfsfunktion in `plant-search.ts`, genutzt in Suchergebnissen und Detail-Import — bevorzugt `P1843[de]`, dann `labels.de` (nur falls nicht identisch zum lateinischen Namen), dann `P1843[en]`, dann `labels.en` (gleiche Prüfung), zuletzt Fallback auf die alten Rohwerte (kein Rückschritt zu leer). Live verifiziert: Tagetes-patula-Suche liefert jetzt „French marigold" statt der Namensdopplung, Cynara cardunculus weiterhin korrekt „Wilde Artischocke". Für Arten ganz ohne jeglichen Trivialnamen (weder de noch en) bleibt die Namensdopplung als letzter Ausweg bestehen — das ist eine echte Datenlücke bei Wikidata selbst, nicht im Code behebbar.
- [x] **„Lade alle fehlenden Daten" lädt alle Pflanzen neu, nicht nur fehlende** — bestätigter Bug: `btn-enrich-all` in `index.astro` filterte die Pflanzenliste vor dem Anreicherungs-Loop nur nach „hat einen lateinischen Namen", nicht nach Vollständigkeit — dadurch bekam jede Pflanze, auch bereits 100%-vollständige, pro Durchlauf eine echte Wikidata-Such- + Detail-Anfrage und (falls aktiviert) eine PFAF/NaturaDB-Proxy-Anfrage. `enrichPlant()` selbst überschreibt zwar nie befüllte Felder (befüllt nur leere), das verhinderte aber keine der Netzwerk-Anfragen — bei 100%-Pflanzen wurde also unnötig PFAF/Wikidata belastet, ohne dass sich je etwas ändern konnte. Fix: neuer Filter `completenessPercent(p) < 100` vor dem Loop (gleiche 10-Feld-Heuristik wie die „Vollst."-Spalte), übersprungene Pflanzen werden gezählt und in der Abschluss-Meldung ausgewiesen („X bereits vollständige Pflanzen übersprungen"); sind alle Pflanzen bereits vollständig, wird der Loop gar nicht erst gestartet. Per Playwright mit Netzwerk-Interception verifiziert: bei einer künstlich auf 100% gebrachten Testpflanze neben einer unvollständigen wurde nur für die unvollständige eine Wikidata-Anfrage ausgelöst (Anfrage-URL enthielt nur deren Namen), die vollständige blieb unangetastet — im bestehenden Live-Datenbestand des Dev-Servers waren dabei bereits alle „echten" Pflanzen vollständig, nur die synthetische Testpflanze wurde verarbeitet (kein ungewollter Seiteneffekt auf echte Daten).
- [x] **Tabelle zu breit, muss scrollen** — durch die vielen Spalten (inzwischen 14 inkl. der neuen Sonne/Wasser-Spalten) überschritt die Desktop-Tabelle regelmäßig die feste Seitenbreite (`max-w-6xl` = 1152px, gemeinsam mit allen anderen Seiten über `Layout.astro` gesetzt) und brauchte horizontales Scrollen. Fix: neuer optionaler `wide`-Prop auf `Layout.astro`, der Header/Main/Footer-Container gemeinsam auf `max-w-[1600px]` statt `max-w-6xl` setzt (nur wenn explizit gesetzt, Standard bleibt unverändert) — aktiviert für `index.astro`, alle anderen Seiten (Hilfe, Datenschutz, Polykulturen, …) bleiben bei der schmaleren, für Fließtext besser lesbaren Breite. Per Playwright bei 1280/1366/1440/1600/1920px Viewportbreite verifiziert: Tabellen-Container braucht bei keiner dieser gängigen Desktop-Breiten mehr Scrollen (`scrollWidth === clientWidth`), Hilfe-Seite bleibt unverändert bei 1152px.
- [x] **Halesia carolina, Zanthoxylum piperitum etc. bleiben auch nach „ergänzen" lateinisch** — Folgefehler zum P1843-Fix oben, nicht derselbe Bug: `commonName` wird von der Anreicherung nur befüllt, wenn das Feld leer ist, nie überschrieben — bei Pflanzen, die *vor* dem P1843-Fix schon einmal angereichert wurden, steht dort aber bereits der (damals fälschlich gespiegelte) lateinische Name als vermeintlich „befüllter" Wert. Erneutes Klicken auf „ergänzen" änderte daher nichts, weil das Feld aus Sicht der Fülllogik nicht leer war — obwohl inhaltlich wertlos. Fix: neue `isFillable()`-Prüfung in `enrichPlant()` (`index.astro`) behandelt `commonName` zusätzlich als befüllbar, wenn der aktuelle Wert exakt (case-insensitive) dem lateinischen Namen entspricht — lässt eine spätere, bessere Quelle (PFAF oder ein erneuter Wikidata-P1843-Treffer) den Wert doch noch ersetzen, ohne eine einmalige Migration zu brauchen. Schützt dabei explizit manuell eingetragene Werte (`_sources.commonName === 'manual'`) vor Überschreiben, auch falls die zufällig dem lateinischen Namen entsprechen. Live per Playwright verifiziert: eine künstlich in den „gespiegelten" Zustand versetzte Testpflanze „Halesia carolina" wird nach Bulk-Ergänzen korrekt zu „Carolina silverbell" (Wikidata P1843/en, da kein P1843/de existiert — Zanthoxylum piperitum hat ebenfalls kein P1843/de, aber PFAF liefert dafür „Japanese Pepper Tree"), eine als `manual` markierte gespiegelte Testpflanze bleibt beim erneuten Ergänzen unverändert.
- [x] **Gartenplan im Browser speichern, so dass er beim erneuten Aufruf wieder verfügbar ist** — Speichern war bisher rein manuell (`btn-save-plan`), erforderte zudem einen ausgefüllten Namen (sonst Alert-Block) — Reload, Absturz oder versehentliches Schließen des Tabs während des Zeichnens/Platzierens verlor den kompletten Fortschritt. Neues debouncetes Autosave (`scheduleAutosave()` in `gartenplan.astro`, 500ms) läuft über `saveGardenPlan()` nach jeder inhaltlichen Änderung: Fläche fertig zeichnen, Pflanze platzieren/ziehen/löschen (2D und 3D), Jahres-Regler, Notizfeld, sowie neu verdrahtete `input`/`change`-Listener auf Name/Beschreibung/Notizen/Polykultur-Auswahl (vorher flossen diese Formularfelder nur beim expliziten Speichern/Export in `editing`, Autosave hätte sonst einen leeren Namen persistiert). Zusätzlich merkt sich `localStorage` (`pgd-gartenplan-last-id`) den zuletzt geöffneten Plan beim Öffnen des Editors und löscht den Eintrag beim bewussten „Zurück zur Liste" — beim erneuten Aufruf von `/gartenplan` springt die Seite bei vorhandenem Eintrag direkt zurück in den Editor, respektiert aber eine bewusste Rückkehr zur Liste (kein erzwungenes Wiederöffnen). Per Playwright verifiziert: Fläche gezeichnet + Pflanze platziert + Name getippt, kein Klick auf „Speichern" — Plan liegt dennoch vollständig in IndexedDB, Reload springt direkt zurück in denselben Editor mit korrektem Namen; separater Test bestätigt, dass ein bewusstes „Zurück zur Liste" nach Reload auch bei Reload auf der Liste bleibt statt zurück in den Editor zu springen.
- [x] **Beim Zeichnen des Gartenplans: Meter-Beschriftung am Raster** — X/Y-Rasterlinien in Phase A (Fläche zeichnen) und Phase B (Platzierung) waren unbeschriftet, die reale Größe eines Bereichs ließ sich nur durch Auszählen der Kästchen abschätzen. Neue `gridTickLabels()`-Funktion (genutzt vom gemeinsamen `gridDefsAndBackground()`, das beide Phasen bereits teilen) beschriftet jede Rasterlinie am oberen Rand (X, Meterzahl) und am linken Rand (Y, Meterzahl) mit der jeweiligen Distanz in Metern, `pointer-events="none"` auf der Beschriftungs-Gruppe verhindert jede Interferenz mit den bestehenden Klick-/Zieh-Handlern für Grenz-Zeichnen und Pflanzen-Platzierung. Per Playwright verifiziert: bei Standard-10×10m-Fläche mit 1m-Raster erscheinen korrekt 20 Beschriftungen (10 X- + 10 Y-Linien) in Phase A und Phase B.
- [x] **Gartenplan-Name als Pflicht einstellen, sonst geht's nicht weiter** — Name war bisher nur beim expliziten „Speichern" verpflichtend (Alert-Block), „Fläche zeichnen starten" (der erste inhaltliche Schritt) ließ sich davor beliebig ohne Namen anklicken — durch das neue Autosave (siehe oben) wären dadurch auch unbenannte Entwürfe dauerhaft persistiert worden. Fix: gleiche Namens-Pflichtprüfung jetzt zusätzlich vor „Fläche zeichnen starten" (`btnStartDrawing`-Handler in `gartenplan.astro`) — ohne ausgefüllten Namen erscheint der bestehende Alert-Hinweis, Fokus springt ins Namensfeld, Zeichnen-Modus wird nicht aktiviert. Per Playwright verifiziert: Klick ohne Namen blockiert (Zeichenbereich bleibt verborgen, Fokus im Namensfeld), nach Eintragen eines Namens funktioniert der gleiche Klick wie erwartet.
- [x] **Neues Feld „Sortenname (selbstgewählt)" bei Grunddaten** — neues `varietyName`-Feld im `PlantData`-Typ (`types.ts`), rein manuell (kein Wikidata/PFAF füllt es, ist explizit für die eigene, selbstgewählte Sortenbezeichnung des Nutzers gedacht, z.B. „Black Krim" bei einer Tomate). In der Bearbeiten-Dialog-Grunddaten (`index.astro`) direkt unter „Deutscher Name" eingefügt — da Formular-Felder generisch über ein gemeinsames `textFields`-Array gelesen/geschrieben werden, reichte das Hinzufügen des Feldnamens dort plus dem `<input id="f-varietyName">` für vollständige Anbindung inkl. automatischer „manuell bearbeitet"-Quellenmarkierung. Per Playwright verifiziert: Feld sichtbar im Bearbeiten-Dialog, Eingabe wird gespeichert und korrekt als `_sources.varietyName: 'manual'` markiert.
- [x] **PDF von „Streifen" war tatsächlich das PDF der Pflanzenkarten (Poly)** — echter Bug, nicht nur eine Falschbenennung: `exportCardsPDF()` in `pdf-export.ts` erzeugte immer BEIDE Kartentypen in einem PDF (erst eine Portrait-A4-Seite Pflanzenkarten, danach die Streifen-Seiten) — unabhängig davon, ob „Poly" oder „Streifen" als Kartentyp ausgewählt war. Alle Aufrufer (`btn-pdf-all`/`btn-bulk-pdf` in `index.astro`, „PDF alle" in `cards.astro`) unterschieden nur `baumscheibe` vs. „alles andere", fielen bei „Streifen" also in denselben kombinierten Export — wer Streifen exportierte, bekam zuerst (und meist überwiegend) Pflanzenkarten zu sehen. Gleicher Bug auch beim Einzelkarten-Export (`exportSingleCardPDF` war fest auf Poly-Kartengröße verdrahtet, ganz ohne Streifen-Variante). Fix: `exportCardsPDF()` aufgeteilt in `exportPolyCardsPDF()`/`exportStripeCardsPDF()`, neue `exportSingleStripeCardPDF()` ergänzt, alle drei betroffenen Aufrufstellen unterscheiden jetzt explizit `poly`/`stripe`/`baumscheibe` statt nur `baumscheibe` vs. Rest. Per Playwright verifiziert (echte PDF-Seitenmaße per `pdf-lib` nachgemessen, nicht nur Dateiname angenommen): Bulk-Export im Streifen-Modus liefert genau eine Landscape-A4-Seite (841,9×595,3pt), Einzelkarten-Export im Streifen-Modus liefert eine 822×48pt-Seite (Streifen-Maß) — beides ohne jede Poly-Seite; Regressionstest bestätigt Poly-Export weiterhin korrekt als reine Portrait-A4-Seite (595,3×841,9pt) ohne Streifen-Seite.
- [x] **Malus domestica wird auf Englisch übersetzt beim Ergänzen — warum?** — live gegen die Wikidata-API verifiziert: das Wikidata-Objekt zu Malus domestica (Q18674606, exakt das von der deutschen Wikipedia unter „Kulturapfel" verlinkte) hat weder ein echtes `labels.de` noch `labels.en` (beide auf den lateinischen Namen gespiegelt) noch überhaupt einen deutschen oder englischen P1843-Trivialnamen (nur cs/fi/vec/ja/zh/sl sind hinterlegt) — ein echter Datenlücken-Fall bei Wikidata selbst, kein Fehler in unserer Auswahllogik. Die vorherige `bestCommonName()`-Priorisierung griff deshalb korrekt auf PFAFs einzige verfügbare Angabe zurück: „Apple" (Englisch) — technisch richtig implementiert, aber unbefriedigend für eine deutschsprachige App, wenn Wikidata selbst zwar keinen Label-Trivialnamen, aber sehr wohl einen von Menschen gepflegten deutschen Wikipedia-Artikeltitel dafür hat („Kulturapfel"). Fix: `bestCommonName()` bezieht jetzt zusätzlich den deutschen Wikipedia-Interwiki-Link (`sitelinks.dewiki.title`, per `sitefilter=dewiki` in beiden Wikidata-Abfragen in `plant-search.ts` mitgeladen) als weitere Fallback-Stufe ein — rangiert nach P1843/de und echtem `labels.de`, aber vor jeglicher englischen Quelle. Per Playwright verifiziert: Malus domestica liefert jetzt „Kulturapfel" (vorher „Apple"), eine künstlich auf den gespiegelten Zustand gesetzte Testpflanze „Malus domestica" heilt sich beim erneuten Ergänzen korrekt zu „Kulturapfel", Tagetes patula bleibt unverändert bei „French marigold" (dort existiert wirklich kein deutscher Wikipedia-Artikel) und Cynara cardunculus bleibt unverändert bei „Wilde Artischocke" (keine Regression).
- [x] **„bei mir steht immer noch apple" — Nachbesserung zum Kulturapfel-Fix** — der dewiki-Fallback (siehe oben) griff korrekt für neu bzw. erneut angereicherte Pflanzen, aber nicht für bereits vorher angereicherte: `commonName` galt bisher nur als „befüllbar", wenn es leer ODER auf den lateinischen Namen gespiegelt war — ein echter, aber veralteter Wert wie PFAFs „Apple" (Quelle `pfaf`) zählte als „schon befüllt" und wurde von keinem erneuten „Ergänzen" mehr angefasst, selbst wenn Wikidata inzwischen (dank „Kulturapfel") etwas Besseres wüsste. Fix: neue `isCommonNameUpgrade()`-Prüfung, ausschließlich im Wikidata-Schritt von `enrichPlant()` (nie im PFAF-Schritt, sonst könnte Englisch wieder einen guten deutschen Namen verdrängen) — erlaubt das Ersetzen eines nicht-manuellen `commonName`, wenn Wikidatas Antwort selbst nicht nur der eigene gespiegelte Lateinname ist und sich vom aktuellen Wert unterscheidet. Manuell gesetzte Namen bleiben durch die bestehende `_sources`-Prüfung unangetastet. Per Playwright mit vier Szenarien gleichzeitig verifiziert: „Apple" (Quelle pfaf) → „Kulturapfel" (Quelle wikidata); ein bereits korrektes „Wilde Artischocke" (Quelle wikidata) bleibt unverändert; ein manuell gesetzter Name bleibt unverändert; ein PFAF-Name für eine bei Wikidata unbekannte Art wird NICHT durch Wikidatas eigenen (wertlosen) gespiegelten Lateinnamen-Fallback verdrängt (kein Rückschritt).
- [x] **Baumscheibe: Höhe/Breite/Härtegrad in gleicher Schriftart wie der lateinische Name** — Höhe (`heightM`), Breite (`widthM`) und Härtegrad/Klimazone (`climateZone`) waren bisher auf Raleway gestellt (bewusst passend zum deutschen Namen), auf Wunsch jetzt stattdessen auf dieselbe Schriftfamilie wie der lateinische Name (`NAME_BOXES.latinName.fontFamily`, effektiv Georgia serif, da die eigentlich gewünschte „Voice-of-the-Highlander" laut bestehendem Code-Kommentar aus Lizenzgründen nie geladen wird und für alle Textfelder gleichermaßen auf den Serif-Fallback zurückfällt) — Kursivstellung wurde bewusst NICHT mit übernommen, da „gleiche Schriftart" sich auf die Schriftfamilie bezieht, nicht auf den beim lateinischen Namen zusätzlich für wissenschaftliche Namen üblichen Kursivstil. Per Playwright verifiziert: gerenderte SVG-Textelemente für Höhe/Breite/Härtegrad tragen jetzt exakt dieselbe `font-family` wie das injizierte lateinische-Name-Textelement; Screenshot-Vergleich bestätigt visuell konsistente Serif-Schrift.
- [x] testen: **tabellenansicht verbessern** — Nutzung und Funktionen sind jetzt zwei getrennte Spalten (`USAGE_BADGES`/`FUNCTION_BADGES` statt einer kombinierten Liste, dabei auch `fuel`/`mineralFix` ergänzt, die vorher in der Badge-Liste fehlten) mit farbigen Dots statt vollen Pills; neue Spalte „Blüte/Frucht" zeigt eine kompakte 12-Tick-Mini-Leiste (Amber=Blüte, Grün=Frucht, Smaragd=beides — gleiche Farbsprache wie `/kalender`); ausklappbare Farblegende (`<details>`) über der Desktop-Tabelle erklärt alle Dot-/Leisten-Farben. Nebenbei zwei echte Bugs behoben: die alten kombinierten Dots hatten `title=""` (gar kein Tooltip) — jetzt mit echten Labels; und die Dot-Farbe wurde zur Laufzeit per String-Replace aus der Pill-Farbklasse abgeleitet (`bg-amber-100` → `bg-amber-400`), was Tailwinds JIT-Scanner nicht erkennt (nur literal im Quellcode vorkommende Klassen werden generiert) — dadurch waren alle Dot-Farben außer Amber unsichtbar (transparent). Fix: jede Badge-Definition trägt jetzt zusätzlich eine literale `-400`-Dot-Farbklasse. **Nachbesserung:** Legende war nur ab Desktop-Breite (`hidden sm:block`) und eingeklappt sichtbar, dadurch auf Mobile unsichtbar und auf Desktop leicht übersehen — jetzt in Mobile- und Desktop-Listenansicht gleichermaßen eingebunden und standardmäßig aufgeklappt (`open`-Attribut). **Nachbesserung 2:** Dot-/Legendenfarben nutzen jetzt die exakten Farben aus dem Baumscheibe-SVG statt einer generischen Tailwind-Palette — für die 9 Nutzung/Funktionen-Felder mit eigenem Farbkeil in der "color"-Gruppe des Templates (`baumscheibe-mapping.ts` BOOL_FIELDS-Alias, z.B. `'e'` für eatable) wurde der Hex-Wert per Medianfarbe der undurchsichtigen Pixel aus dem jeweiligen PNG-Keil extrahiert (nicht geschätzt); `material`/`fodder`/`fuel` haben keinen Keil im Template (nur 9 von 15 Feldern haben einen, per Bildanzahl in der SVG bestätigt) und behalten daher einen sinnvollen Ersatzwert. Farben als Inline-`style` statt Tailwind-Klasse gesetzt, da beliebige Hex-Werte den JIT-Scanner ohnehin nicht durchlaufen können.

- [x] **Wuchsgeschwindigkeit als Spalte, filter- und sortierbar** — dritte Dot-Spalte „Wuchs" nach Sonne/Wasser (analog implementiert: neues `GROWTH_BADGES`-Tupel-Array, `growSpeedFilter`-Set + Dropdown-Filter nach demselben `sunFilter`/`waterFilter`-Muster, Legende erweitert, Dots in Desktop-Tabelle und Mobile-Karte). Zusätzlich sortierbar, anders als Sonne/Wasser — `growSpeedLow`/`Mid`/`High` sind drei separate Booleans statt eines Ordinalwerts, daher neue `growSpeedRank()`-Hilfsfunktion (Langsam=1/Mittel=2/Schnell=3, nicht gesetzt=Infinity, gleiche „unbekannt sortiert zuletzt"-Konvention wie bei Höhe/Breite) als neuer `sortField`-Zweig. Per Playwright verifiziert: Spalte „Wuchs ↕" erscheint sortierbar zwischen Wasser und Blüte/Frucht, aufsteigende Sortierung liefert Langsam→Mittel→Schnell→unbekannt, Filter „Schnell" blendet alle anderen Geschwindigkeiten korrekt aus. **Nachbesserung:** Farben auf Wunsch von der ursprünglichen grünen Abstufung auf echte Ampelfarben umgestellt (Langsam=Rot `#ef4444`, Mittel=Amber `#f59e0b`, Schnell=Grün `#22c55e`) — per Playwright an den tatsächlich gerenderten Dot-Hintergrundfarben verifiziert.
- [x] **Baum/Strauch/Krautebene als Spalte, filter- und sortierbar** — neue Spalte „Ebene" nach Wuchs, nutzt die bereits im Gartenplan vorhandene `deriveLayer()`-Heuristik aus `plant-layer.ts` (aus `heightM` + `groundCover` abgeleitet, kein echtes Datenbankfeld — siehe Kommentar dort) statt einer neuen Berechnung, damit Tabelle und Gartenplan garantiert dieselbe Klassifizierung zeigen. Anders als Sonne/Wasser/Wuchs ist hier immer genau ein Wert pro Pflanze gesetzt (kein Mehrfachauswahl-Bool-Set), daher neue `layerDot()`-Funktion (ein einzelner Dot statt einer Dot-Gruppe) statt Wiederverwendung von `dotGroup()`; Farben identisch zu `LAYER_STYLE` aus dem Gartenplan (Baum dunkelgrün, Strauch oliv, Kraut hellgrün) für visuelle Konsistenz zwischen beiden Ansichten. Filter-Dropdown nach demselben Muster wie die übrigen Spalten (`layerFilter`-Set, ODER-Logik bei Mehrfachauswahl). Sortierbar über neue `LAYER_RANK`-Zuordnung (Kraut=1/Strauch=2/Baum=3) als weiterer `sortField`-Zweig — `deriveLayer()` liefert im Gegensatz zu Wuchsgeschwindigkeit immer einen Wert, daher kein „nicht gesetzt"-Sonderfall nötig. Per Playwright verifiziert: Spalte „Ebene ↕" korrekt einsortiert, Dot-Farben/Tooltips stimmen für alle drei Ebenen (Testpflanzen mit heightM 8m/1,5m/0,2m), aufsteigende Sortierung liefert Kraut→Strauch→Baum, Filter „Baum" blendet Strauch/Kraut korrekt aus.
- [x] **Neu konvertierte Baumscheibe-SVG als Template integriert (`pwa/public/baumscheibe-template.svg`)** — ersetzt die bisherige, aus einer früheren Konvertierung stammende Datei durch die vollständige Neukonvertierung (siehe `psd2svg/psd2svg-anleitung.md`). Zwei Elemente existierten nur im alten Template als nachträgliche Handarbeit, nicht als PSD-Ebenen, und mussten gezielt übernommen werden statt sich aus der Neukonvertierung zu ergeben: `commonName`/`latinName` sind echte `<text>`-Platzhalter aus der PSD (per exaktem Text+Position-Abgleich neu gelabelt, keine `<image>`-Platzhalter wie ein alter Code-Kommentar fälschlich behauptete — dieser wurde korrigiert) und `heightM`/`widthM`/`climateZone` sind gar keine PSD-Ebenen (manuell in Inkscape nachgetragen) — 1:1 aus dem alten Template kopiert. Beim ersten Integrationsversuch (alle vorher versteckten Ebenen pauschal sichtbar geschaltet) zwei echte Regressionen gefunden und behoben, bevor sie live gingen: (1) ein Renderfehler zeigte einen kompletten zusätzlichen Icon-Cluster unterhalb der Karte — verursacht durch `symbols_functions`/`symbols_uses`, deren einzeln ausgeblendete Kind-Ebenen (u_edible2, u_tea, u_fodder2/3, u_dye2, u_crafts, f_wind2, f_dynacc2, …) sich als verworfene Entwurfs-Duplikate herausstellten, nicht als Varianten-Zustände wie zunächst angenommen — durch gezielten Vergleich der Sichtbarkeits-Flags jeder einzelnen Kind-Ebene identifiziert, nicht geraten. (2) `layer`/`growth speed`/`water`/`light`/`rating2` wurden pauschal komplett sichtbar geschaltet, was bei mehreren gesetzten Icon-Zuständen zu Überlagerungen führte, da der bestehende Code diese Gruppen nur als Ganzes ein-/ausblenden konnte (`setGrowthSpeedIcon()`) oder gar nicht ansteuerte (`layer`) — für `layer`/`growth speed` echte Einzelzustands-Ansteuerung ergänzt (`setLayerIcon()` nutzt dieselbe `deriveLayer()`-Heuristik wie Tabelle/Gartenplan; `setGrowthSpeedIcon()` zeigt jetzt gezielt `speed1`/`speed2`/`speed3` statt aller drei gleichzeitig — löst nebenbei die bisherige „Behelfslösung"-Einschränkung auf), `water`/`light`/`rating2` bewusst auf den ursprünglichen Einzel-Vorschau-Zustand zurückgesetzt (keine 1:1-Zuordnung zu den PlantData-Feldern ohne Rückfrage beim Designer, PFAF-Sterne-Rendering existiert noch nicht). Per Playwright verifiziert: alle bisherigen Nutzung/Funktionen/pH-Icons unverändert funktionsfähig (Label-Auflösung 1:1 gegen altes Template abgeglichen), Wuchsgeschwindigkeit und Ebene zeigen jetzt korrekt genau ein passendes Icon je Zustand (6 Testfälle), Namen/Maße/Klimazone korrekt eingeblendet ohne sichtbaren alten Platzhaltertext dahinter, alle vier PDF-Export-Pfade (Einzelkarte, leere Pflanze als Regressionstest, Bulk, 6er-Sheet) fehlerfrei.
- [x] **DSGVO-Härtung: Google Fonts lokal, keine IP-Logs, Security-Header** — Raleway wurde bisher von `fonts.googleapis.com` geladen (Übermittlung der Besucher-IP an Google, Abmahnrisiko nach LG München 3 O 17493/20) und ist jetzt selbst gehostet (`public/fonts/*.woff2`, `@font-face` in `global.css`, OFL-lizenziert; Latin + Latin-Ext). Plausible-`data-domain` von der alten Netlify-Domain auf `permadesignkit.org` korrigiert (muss zusätzlich im Plausible-Dashboard registriert werden). nginx auf dem VPS: `access_log off` (global + Site), `error_log` nur noch `crit`, bereits angefallene Logs gelöscht — es werden keine IP-Adressen mehr gespeichert; der Plant-Proxy hält IPs nur für das 60-Sekunden-Rate-Limit im Arbeitsspeicher (nicht geloggt, per Code geprüft). Zusätzlich HSTS (6 Monate, ohne preload), `X-Content-Type-Options`, `X-Frame-Options`, `Referrer-Policy`, `Permissions-Policy`. Datenschutzerklärung (Abschnitt Hosting, DE/EN + statischer Fallback) an den tatsächlichen Stand angepasst, Stand-Datum aktualisiert. Per Live-Check verifiziert: keine `googleapis`-Referenz mehr im ausgelieferten HTML, Schriftdatei und Header kommen an. **Bewusst nicht gemacht:** Content-Security-Policy (App nutzt Inline-Skripte, Plausible und mehrere externe APIs — ohne Test riskant). **Weiterhin offen:** Impressum-Platzhalter ausfüllen (Angaben nur vom Betreiber), Wikimedia-Bildlizenzen/Urheberangabe prüfen.
- [x] **Impressum ausgefüllt** — Anbieter/Kontakt eingetragen (Name, Anschrift, E-Mail; keine Telefonnummer, die Zeile wurde entfernt), Platzhalter-Kommentar entfernt. Rechtsgrundlage von „§ 5 TMG" auf „§ 5 DDG" (Digitale-Dienste-Gesetz, ersetzt das TMG seit Mai 2024) umgestellt, ebenso die Haftungs-Paragraphen (§§ 7–10 DDG), DE und EN. Live geprüft: keine Platzhalter mehr im ausgelieferten HTML.

### Debugging

#### Darstellung Scheibe & mapping prüfen
- [x] **raleway schrift auch für höhe und durchmesser und winterhärte** — `heightM`/`widthM`/`climateZone` waren noch mit der alten Template-Schrift (Inter, bold) gerendert; jetzt per `el.style.fontFamily` auf Raleway umgestellt, gleiche Google-Fonts-Schriftart wie `commonName`. Font-Load explizit und unbedingt in `renderBaumscheibeSvg()` erzwungen (`document.fonts.load()`), unabhängig davon ob `commonName`/`latinName` gesetzt sind — `injectNameText()` lädt die Schrift sonst nur, wenn der jeweilige Namens-Wert nicht leer ist.

#### weitere bugs / validierung
- [x] **NOW anzahl pflanzen auf karten-seite** — der bisher nur in Kachel-/Listenansicht (`index.astro`) vorhandene Druckanzahl-Stepper ist jetzt auch auf `/cards` und in der Kartenansicht von `index.astro` verfügbar. `printStepperHtml()` dafür nach `card-html.ts` verschoben (nimmt jetzt eine `t`-Funktion als Parameter, damit jede Seite ihr eigenes i18n-Dict nutzen kann), Klick-Handler in `cards.astro` als Teil der bestehenden Event-Delegation ergänzt (adressiert über `data-index` statt `data-id`, da `cards.astro` Pflanzen indexbasiert referenziert)

### Gartenplan (neu)
- [x] **Gartenplan-MVP** — neue Seite `/gartenplan`, neuer Reiter neben Polykulturen. Nutzer zeichnet eine Fläche als frei geklicktes Polygon (mit Raster-Snapping, kein festes Rechteck), platziert Pflanzen manuell per Klick/Tap darauf (kein Auto-Placement) und zieht sie per Pointer-Events (Maus + Touch) frei. Ein Schieberegler „Jahre seit Pflanzung" (0-30) skaliert jeden Pflanzenkreis live über ein selbst entwickeltes Wachstumsmodell (`growth-model.ts`) — PFAF liefert keine Alter-zu-Größe-Kurve, nur finale `heightM`/`widthM` plus grobe Wachstumsgeschwindigkeit, daher eine explizit als Heuristik gekennzeichnete logistische Kurve (Reifealter schnell≈6/mittel≈15/langsam≈30 Jahre, erreicht ~95% der finalen Größe am Reifealter). Koordinaten durchgehend in Metern gespeichert (`GardenPlan`/`GardenPlanPlacement` in `types.ts`), SVG-Rendering mit `viewBox` (100 SVG-Einheiten = 1m) statt manueller px/m-Umrechnung. Eigener IndexedDB-Store `gardenPlans` (DB v5, additiv/idempotent wie die bisherigen Versionssprünge). Optionale Verknüpfung mit einer bestehenden Polykultur: einmaliger, per Button ausgelöster Import ihrer Mitglieder als unplatzierte Liste — bewusst **keine** laufende Synchronisierung (verifiziert: nachträgliches Hinzufügen eines Mitglieds zur Polykultur taucht im gespeicherten Gartenplan nicht auf). Getestet per Playwright: Fläche zeichnen, Pflanzen platzieren/ziehen (Maus und Touch-Emulation), Schieberegler-Skalierung (Größenverhältnis gegen die Wachstumsformel gegengerechnet, nicht nur "größer als vorher"), Speichern/Neuladen inkl. Persistenz von Grenze/Platzierungen/Schieberegler-Stand, Polykultur-Verknüpfung ohne Sync, Englisch-Sprachumschaltung.
- [x] **Gartenplan: realistischere Darstellung** — Kreise ersetzt durch deterministisch generierte unregelmäßige „Blob"-Umrisse (`blob-shape.ts`, seedbasiert auf der Placement-ID, also über Re-Renders/Reloads stabil statt bei jedem Render neu zu wackeln) — kein echtes artgenaues Silhouetten-Set (existiert nicht), aber deutlich organischer als ein perfekter Kreis. Baum/Strauch/Kraut-Ebene wird aus `heightM` (+ `groundCover` als Tie-Breaker) abgeleitet (`plant-layer.ts` — es gibt kein echtes Ebenen-Feld in `PlantData`, trotz gegenteiliger Andeutung in CLAUDE.md) und bekommt je eigene Farbe + Wackel-Stärke; kleine immer sichtbare Farblegende unter der Karte. Pflanzenname wird jetzt permanent unter jeder Form angezeigt (nicht mehr nur bei Auswahl/Hover), mit weißem Textumriss (`paint-order: stroke`) für Lesbarkeit über Raster/Füllfarbe. Platzierungen sind jetzt `<g transform="translate(…)">`-Gruppen statt einzelner `<circle>`-Elemente — Drag verschiebt nur den Transform, der Schieberegler regeneriert den Blob-Pfad (`d`-Attribut) und die Label-Position pro Tick, weiterhin ohne vollständiges Re-Render. Neue einklappbare Infobox erklärt Bedienung, Wachstumsmodell-Heuristik und die Ebenen-/Formvereinfachung transparent.
- [x] **Gartenplan: Backup/Restore-Integration** — `gardenPlans` jetzt in `sync.ts`s `buildBackupJson()` sowie allen vier Restore-Pfaden in `settings.astro` (WebDAV-Pull, File-System-Access-Read, Gist-Pull, JSON-Datei/Paste-Restore), inkl. aktualisierter Bestätigungs-/Status-/Toast-Texte mit Gartenplan-Anzahl. Spiegelt exakt das bestehende `polycultures`-Handling.
- [x] **Gartenplan: Auto-Platzierung / Vorschläge** — `compatScore()` aus `polykulturen.astro` in `lib/compat.ts` extrahiert (jetzt von beiden Seiten genutzt, keine Doppelimplementierung mehr). Neues „Vorschläge"-Panel in Phase B: rankt unplatzierte Bestandspflanzen nach durchschnittlichem Sonne/Wasser/pH-Überlappungs-Score gegen alle bereits platzierten Pflanzen, Top 5 mit Score/3 anzeigt. Bewusst **kein** Auto-Placement — ein Vorschlag landet nur in der (jetzt neutral „Noch zu platzieren" benannten, da sie auch aus Vorschlägen befüllt wird, nicht nur aus dem Polykultur-Import) Warteliste, die Positionierung bleibt manuell.
- [x] **Gartenplan: 3D-Ansicht mit vollem Editing** — neuer 2D/3D-Umschalter in Phase B (Toggle, kein Splitscreen — beide Ansichten bearbeiten dieselben `editing.placements`). Three.js per dynamischem `import()` nachgeladen (gleiches Lazy-Chunk-Muster wie `pdf-lib`), neues Modul `gartenplan-3d.ts`. Bäume/Sträucher/Kräuter als prozedurale Formen (Stamm+gejitterte Icosahedron-Krone / gestauchte Icosahedron / flacher Zylinder), Farben identisch zu `LAYER_STYLE`, Größe über dasselbe `displayRadiusM()`. Platzieren/Ziehen direkt in 3D per Raycasting: eigener `pointerdown`-Listener in der **Capture-Phase** auf dem Container (läuft garantiert vor `OrbitControls`' eigenem Listener auf dem Canvas, verifiziert im `OrbitControls`-Quellcode) — bei Treffer auf eine Pflanze wird `controls.enabled=false` gesetzt (OrbitControls prüft das als Erstes und reagiert dann gar nicht erst), sonst läuft die Kamera-Steuerung unverändert. `pointInPolygon` und `seededRandom` aus den bestehenden 2D-Dateien exportiert/extrahiert (`gartenplan-geometry.ts`, `blob-shape.ts`) statt dupliziert. Boundary-Fläche per `ShapeUtils.triangulateShape` trianguliert, `DoubleSide`-Material statt Winding-Erkennung (Klick-Reihenfolge beim Zeichnen ist beliebig). Per Playwright verifiziert: WebGL-Context lädt (funktioniert im Headless-Sandbox ohne Sonderflags), Platzieren in 3D funktioniert, mehrfaches Umschalten 2D↔3D erzeugt keinen Canvas-/Kontext-Leak. **Nachgetestet:** Platzieren in 3D → Speichern → Neuladen → in 3D ziehen → Speichern funktioniert sauber (Position ändert sich und persistiert, keine JS-Fehler). **Touch in 3D ebenfalls verifiziert** (iPhone-13-Emulation): Fläche per Touch-Taps zeichnen, Pflanze per Touch-Tap in 3D platzieren, per Touch-Drag verschieben — Position ändert sich und persistiert. Entscheidender Nachweis, dass der Capture-Phase-Trick greift: Pixel-Vergleich der Canvas-Screenshots vor/nach dem Touch-Drag zeigt nur **0,13 % Änderung**, eng begrenzt auf den Bereich der bewegten Pflanze — hätte `OrbitControls` die Ein-Finger-Geste abgefangen, wäre die Kamera mitgerotiert und praktisch das gesamte Bild verändert.
- [x] **Gartenplan: Detailinfos beim Klick auf eine Pflanze** — das „Ausgewählt"-Panel zeigt jetzt dieselben Infos wie eine Pflanzenkachel (Bild, deutscher + lateinischer Name, Höhe/Breite/Zone, Nutzung- und Funktionen-Badges, Blüte/Frucht-Leiste, Vollständigkeitsbalken) statt nur des Namens — in 2D und 3D identisch. Neue geteilte Bausteine `lib/plant-detail.ts` (Badge-Definitionen, `completenessPercent()`, `plantDetailHtml()`) und `i18n/dict-plant-detail.ts`; Badge-Definitionen und -Labels lagen vorher seiten-lokal in `index.astro` und wurden dorthin verschoben, sodass Kachel und Gartenplan garantiert dieselben Texte/Farben nutzen (eine zweite Kopie wäre sonst auseinandergelaufen). Bewusst **ohne** die Kachel-Aktionen (Ergänzen/Bearbeiten/Löschen/Druckanzahl) — das sind Pflanzenverwaltungs-Funktionen, im Gartenplan wird die Pflanze nur referenziert. Regression geprüft: Badges und Tabellenüberschriften auf der Pflanzenseite unverändert korrekt.
- [x] **Gartenplan: PDF-/Druckexport** — neuer Button „PDF exportieren" in Phase B. Rendering-Logik (Raster/Umrandung/Blob-Marker) aus `gartenplan.astro` in eine reine, DOM-freie Funktion extrahiert (`gartenplan-render.ts`), damit die Live-Ansicht und der Export dieselbe Darstellung garantiert nicht auseinanderlaufen lassen (nur die Drag-Interaktivität bleibt Seiten-lokal, für einen statischen Export nicht nötig). Seitenformat (Hoch-/Querformat) wird aus dem Seitenverhältnis der Fläche gewählt, Titel/Zeitpunkt-Bildunterschrift/Ebenen-Farblegende werden direkt als PDF-Text/-Formen gezeichnet (`pdf-lib` `drawText`/`drawEllipse`), die Fläche selbst als JPEG eingebettet (`svgStringToCanvas`/`canvasToJpegBytes`, gleiches Muster wie beim Baumscheibe-Export — hier ohne Firefox-Sonderpfad, da die Gartenplan-SVG rein vektoriell und winzig ist, nicht wie das 5-MB-Baumscheibe-Template). Nebenbei einen echten Bug gefunden und behoben: Name/Beschreibung/Notizen wurden nur beim Speichern aus den Formularfeldern in `editing` übernommen — beim Export **vor** dem ersten Speichern landete dadurch der getippte Name weder im PDF-Titel noch im Dateinamen. Fix: gemeinsame `syncFormIntoEditing()`-Funktion, jetzt vor Speichern **und** vor Export aufgerufen.

### Baumscheibe Karte Feinschliff
- [x] **SVG neu generiert: `baumscheibe2.3_inkl-label.svg`** — farbiger, überarbeiteter Export ersetzt `pwa/public/baumscheibe-template.svg`. Rohe Photoshop-Ebenennamen (`u_edible`, `f_Nfixer`, `ph2_acid`, …) statt bisheriger PlantData-Feldnamen als `inkscape:label` — Zuordnung per Positionsvergleich ermittelt (`getBBox()` alt vs. neu, nächster Nachbar, meist < 5px), nicht geraten; Details im Kommentar oben in `baumscheibe-mapping.ts` und in `baumscheibe-mapping-status.md`. Disc-Geometrie und Frucht-/Blüte-Ringe per Pixel-Sampling erneut vermessen: byte-identisch zur alten Version, keine Code-Anpassung an `pdf-export.ts`/`baumscheibe-render.ts` nötig. `latinName`/`commonName`/`heightM`/`widthM`/`climateZone` existierten im rohen Export nicht (wie schon vorher reine manuell nachgetragene Textfelder) — 1:1 aus dem alten Template übernommen. **Bonus:** `phVeryAcid`/`phVeryAlkaline` sind jetzt erstmals verdrahtet (2.3-Template hat alle 5 pH-Stufen statt bisher 3). **Regression:** `growSpeedMid`/`growSpeedHigh` sind jetzt tot (2.3-Template hat nur noch 1 statt 3 Wachstumsgeschwindigkeits-Icons) — neuer Punkt in `baumscheibe-mapping-status.md`. Getestet: volle Testpflanze, leere Testpflanze, alle drei PDF-Export-Pfade (Einzelkarte/Bulk/6er-Sheet) per Playwright + Sichtprüfung der gerenderten PDFs
- [x] **Schriftarten von SVG übernommen (commonName)** — `NAME_BOXES` in `baumscheibe-render.ts` nutzt jetzt Raleway (wie das Template selbst für „COMMON NAME") statt Inter; per Google Fonts geladen (OFL-lizenziert), `document.fonts.load()` vor dem Rendern erzwungen, da ein per `Image()`-Blob geladenes SVG das Nachladen sonst nicht selbst auslöst — sonst hätte der PDF-Export (Canvas-Rasterung) die Schrift verpasst, obwohl sie im Hauptdokument schon registriert war. Per `document.fonts.check()` und Sichtprüfung des gerenderten PDFs bestätigt. **latinName offen:** das Template nutzt „Voice-of-the-Highlander", ein Schriftschnitt von Octotype/DaFont — laut Lizenz nur für den persönlichen Gebrauch frei, für Web-Embedding in dieser App bräuchte es eine kommerzielle Lizenz (Kontakt: octotypeone@gmail.com). Font-Family wird zwar angefragt (fällt aktuell auf eine Serif-Kursive zurück), aber nicht eingebunden — Kauf/Klärung der Lizenz wäre nötig, bevor sie echt eingebunden werden kann
- [x] **Wachstums-Icon aktiviert (Behelfslösung)** — die "growth speed"-Gruppe hat nur ein generisches Icon statt drei einzelner (Low/Mid/High); `setGrowthSpeedIcon()` zeigt es, sobald *irgendeines* von `growSpeedLow`/`Mid`/`High` gesetzt ist (kann die drei Stufen also nicht unterscheiden). Echte Mid/High-Icons ergänzen bleibt offen, falls das Artwork erweitert wird
- [x] **Farbflächen folgen jetzt den Icons** — die "color"-Gruppe (10 farbige Hintergrund-Flächen hinter Essbar/Medizin/Kulinarisch/N-Fixer/Mineralien/Bodendecker/Insekten/Tierschutz/Windschutz) war unabhängig von den Icons immer sichtbar; jetzt als zusätzliche Label-Aliase in `baumscheibe-mapping.ts` ergänzt, sodass Aus-/Einblenden eines Feldes Icon *und* Farbfläche gemeinsam steuert
- [x] **Frucht-/Blütemonate auf echtes Label-Mapping umgestellt** — der bisherige Hardcode-Workaround (Donut-Segmente per Pixel-Sampling über die Ring-Geometrie gelegt) ist ersetzt: das 2.3-Template hat gelabelte Gruppen `harvest` (Frucht) und `flowering` (Blüte) mit je einem `<image>` pro Monat; DOM-Reihenfolge der Kind-Elemente entspricht der Winkel-Reihenfolge (per `getBBox()` bestätigt, nicht angenommen). `setMonthRing()` blendet sie einzeln nach `fruitMonths[i]`/`flowerMonths[i]` ein/aus, statt neue Pfade zu injizieren. Besonderheit: `harvest` hat nur 11 statt 12 Bilder (kein Frucht-Icon für April) — per Winkel-Abgleich gegen `flowering`s vollständige 12 Positionen bestimmt, nicht geraten. Getestet: alle Monate einzeln, der April-Sonderfall, ein realistisches Apfel-Muster, der Grenzfall „Frucht+Blüte im selben Monat", alle drei PDF-Export-Pfade. Details in `baumscheibe-mapping-status.md`

### sonst
- [x] **Sanduhr/Spinner bei PDF-Erstellung** — `withButtonSpinner()` (neu in `src/lib/html.ts`) deaktiviert den Button und zeigt einen kleinen drehenden Kreis + Label während der Generierung, auf allen PDF-Buttons in `index.astro` und `cards.astro` (Einzelkarte, Bulk, 6er-Sheet); stellt den ursprünglichen Inhalt immer wieder her, auch bei Fehlern
- [x] **Baumscheibe als Standardansicht** — `DEFAULT_PREFS.defaultCardVariant` in `settings.ts` von `'poly'` auf `'baumscheibe'` geändert; wirkt nur für neue Nutzer bzw. wenn nie explizit etwas anderes gespeichert wurde

### PDF Generierung
- [x] **Größen angepasst: 6 Baumscheiben pro Seite, 9cm Durchmesser** — neuer Button „PDF (6/Seite, 9cm)" auf `/cards` (nur sichtbar bei Baumscheibe-Ansicht), `exportBaumscheibeSheetPDF()` in `pdf-export.ts`. Das bisherige Einzelkarten-Layout (`exportBaumscheibePDF`/`exportBaumscheibesPDF`) druckt das volle Template-Canvas (193,5×210,2mm, viel Weißraum außerhalb der eigentlichen Scheibe) — dieses neue Layout schneidet stattdessen eng auf die Scheibe zu. Kreismittelpunkt/-radius wurden vermessen (Canvas an 24 Winkeln vom Zentrum nach außen abgetastet, äußerster Tintenpixel je Winkel: konsistent ~1011–1046, ein 1077-Ausreißer durch eine kleine Dekor-Kerbe oben rechts) statt geschätzt. 2×3-Raster auf A4, mittig. Firefox nutzt wie beim bestehenden Export den nativen Druckdialog mit eingebettetem (zugeschnittenem) SVG statt Rasterung. Verifiziert: PDF mit 7 Test-Pflanzen erzeugt (6 auf Seite 1, 1 auf Seite 2), beide Seiten A4 (210×297mm), per `poppler-utils` gerendert und den tatsächlichen Scheibendurchmesser nachgemessen — 89,15mm bei Sollwert 90mm (Abweichung durch Kantenerkennungs-Toleranz, nicht durch falsche Skalierung)
  - **Nachbesserung:** Auf einem echten Android-Handy (Firefox → Android-Systemdruck) war die Seite rechts/unten beschnitten. Ursache: 3 Reihen à ~91,7mm (90mm-Scheibe + Zuschnitt-Puffer) summieren sich auf ~275mm gegen eine 297mm hohe A4-Seite — bei nur 4mm `SHEET_GAP_MM` blieb kaum natürlicher Rand übrig (~7mm), und Android/Firefox' Druck-Pipeline erzwingt beim systemeigenen Druckdialog offenbar einen eigenen Mindestrand, den `@page { margin: 0 }` dort nicht zuverlässig überschreibt (anders als der PDF-lib-Pfad über Chrome/Desktop, der exakte Koordinaten direkt in die PDF-Datei zeichnet und von Browser-Druckrändern gar nicht betroffen ist). `SHEET_GAP_MM` von 4mm auf 1mm reduziert (Kreise brauchen ohnehin keinen sichtbaren Abstand, die Ecken zwischen vier Kreisen sind schon leer) — vergrößert den natürlichen Rand auf ~13mm horizontal / ~10mm vertikal. `@page`/`.page`-Größe im Druckpfad zusätzlich von der Keyword-Form (`size: A4 portrait`) auf explizite mm-Angaben (`size: 210mm 297mm`) umgestellt, da manche Druck-Engines das zuverlässiger respektieren. Erneut vermessen: Scheibendurchmesser unverändert 89,15mm, linker Rand jetzt 13,46mm (vorher ~11mm). **Restrisiko:** Bei einem Android-Gerät, dessen Systemdruck einen noch größeren Mindestrand erzwingt, kann trotzdem noch beschnitten werden — das lässt sich vom Code aus nicht zu 100% garantieren, da der Zuschnitt dann außerhalb unserer Kontrolle (im Android-Druckdienst) passiert. Betroffen ist ausschließlich der Firefox/Android-Druckpfad; der PDF-Direktdownload (z.B. via Chrome) hat dieses Risiko nicht
- [x] **Druckanzahl / Deaktivieren pro Pflanze** — persistiertes Feld `printCount` (Default 1) auf `PlantData`: 0 = deaktiviert (aus allen Bulk-PDF-Exports ausgeschlossen), >1 = Karte wird entsprechend oft wiederholt gedruckt (z.B. 3 für drei geplante Apfelbäume). Stepper-UI (−/Zahl/+) in Kachel- und Listenansicht, Klick auf die Zahl togglet 0/1 als Schnell-Deaktivieren. `exportCardsPDF`/`exportBaumscheibesPDF`/`exportBaumscheibeSheetPDF` expandieren die Pflanzenliste per `expandByPrintCount()` vor der Seiten-Paginierung; Einzelkarten-Export (ein Klick = eine Karte) bleibt bewusst unverändert. Byte-genau verifiziert: `printCount: 3` erzeugt identischen PDF-Output wie dreimaliges manuelles Auflisten derselben Pflanze
- [x] **Fehlender 6er-PDF-Button auf der Startseite** — der „PDF (6/Seite, 9cm)"-Button existierte nur auf `/cards`, nicht in der Kartenansicht der Startseite (`index.astro`). Ergänzt inkl. Sichtbarkeits-Toggle (nur bei Baumscheibe-Ansicht) und Klick-Handler
- [x] **Standard-Kartentyp zwischen Startseite und `/cards` nicht synchron** — `index.astro` initialisierte sein Karten-Dropdown aus `settings.defaultCardVariant`, `cards.astro` tat das nie und startete immer bei „Pflanzenkarten". Beide Seiten respektieren jetzt die gespeicherte Einstellung

### Sprache
- [x] **Übersetzung auf Englisch** — vollständige Mehrsprachigkeit (Deutsch Standard, Englisch), siehe „Mehrsprachigkeit" unten für Details

### Terminologie & Rebranding
- [x] **„Gilde" → „Polykultur"** — durchgängig umbenannt, inkl. Datenmodell: `Guild`/`GuildRole`/`GuildMember`/`createEmptyGuild` in `types.ts` → `Polyculture`/`PolycultureRole`/`PolycultureMember`/`createEmptyPolyculture`; `db.ts`-Funktionen (`getAllGuilds` etc.) → `getAllPolycultures` etc.; IndexedDB-Objectstore `guilds` → `polycultures` (Migration in `upgrade()`: bestehende Datensätze werden beim nächsten DB-Öffnen automatisch in den neuen Store kopiert, alter Store danach gelöscht — kein Datenverlust). Seite `gilden.astro`/`/gilden` → `polykulturen.astro`/`/polykulturen`. JSON-Backup-Feld `guilds` → `polycultures`; Restore-Pfade (WebDAV, File System Access API, GitHub Gist, Datei-Upload) akzeptieren beide Feldnamen für Rückwärtskompatibilität mit alten Backups. **Bewusst NICHT umbenannt:** die IndexedDB-Datenbank selbst heißt technisch weiterhin `permaculture-guilds` (nur der Store innerhalb der DB wurde umbenannt) — eine volle DB-Umbenennung bräuchte eine aufwändigere datenbankübergreifende Migration für null sichtbaren Nutzen, da der Name ohnehin nie im UI auftaucht
- [x] **„Perma Guild Forge" → „Perma Design Kit"** — App-Name überall aktualisiert (Header, Seitentitel, PWA-Manifest `name`/`short_name`, Footer-Copyright, Backup-Dateinamen, READMEs, `ROADMAP.md`). GitHub-Repo umbenannt: `tbdal/Perma-Guild-Forge` → `tbdal/Perma-Design-Kit` (GitHub leitet den alten Link automatisch weiter), lokaler `origin`-Remote entsprechend angepasst. Rückwärtskompatibilität analog zur Gilde-Umbenennung: `localStorage`-Settings-Key und Gist-Backup-Dateiname wurden umbenannt, alte Werte werden beim Lesen als Fallback akzeptiert, sodass bestehende Nutzer ihre Einstellungen/Sync-Konfiguration nicht verlieren. Ordnername auf dem VPS (`/root/pdk`) bewusst unverändert gelassen, um laufende Dienste/Pfade nicht zu brechen

### Sicherheit (Launch-Blocker)
- [x] **Stored-XSS beim `innerHTML`-Rendering fixen** — `escapeHtml()` aus `pdf-export.ts` nach `src/lib/html.ts` verschoben und an allen Stellen angewendet, die Pflanzen-/Polykultur-Nutzerdaten (`commonName`, `latinName`, `imageUrl`, `climateZone`, Polykulturname/-beschreibung, Such-Ergebnislabels, API-Key-Feld) in `innerHTML`-Template-Strings rendern (`index.astro`, `polykulturen.astro`, `cards.astro`, `kalender.astro`, `settings.astro`, `card-html.ts`). Die Baumscheibe-Karte (`baumscheibe-render.ts`) war bereits sicher (`textContent` + `XMLSerializer`). Mit Playwright gegen einen `"><img src=x onerror=alert('XSS')>`-Payload über alle Ansichten (Grid/Liste/Karten, `/`, `/polykulturen`, `/cards`, `/kalender`) verifiziert — kein Alert, Payload erscheint nur als Text. *(Seite/Route seither von `gilden`/`/gilden` auf `polykulturen`/`/polykulturen` umbenannt, siehe „Gilde → Polykultur" unten.)*
- [x] **Credentials in Einstellungen maskieren** — WebDAV-Passwort und GitHub-PAT waren im Code bereits `type="password"`; zusätzlich das API-Key-Feld der Datenquellen (`settings.astro`) gegen Attribut-Escape durch `"` im Wert abgesichert. localStorage bleibt weiterhin Klartext (siehe PocketBase-Option unter Langfristig für eine serverseitige Lösung).

### Lizenz & Datenquellen (Launch-Blocker)
- [x] **NaturaDB-Scraping vorerst deaktiviert** — *(Pfad-Hinweis: `netlify/functions/plant-proxy.mts` existiert seit der Netlify-Migration weiter unten nicht mehr, siehe `server/plant-proxy-server.mjs`)*. Scraped `naturadb.de` ohne erkennbare Lizenz für die redaktionelle Pflanzendatenbank (Nutzungsbedingungen regeln nur nutzergenerierte Inhalte). `robots.txt` enthält einen expliziten `User-agent: Datenbank Crawler` → `Disallow: /`-Eintrag als klares Anti-Scraping-Signal, Betreiber ist die kommerzielle Maseto GmbH. Ohne Lizenz greift das deutsche Datenbankherstellerrecht (§ 87a ff. UrhG) — Abmahnrisiko real, besonders da der Scraping-Code öffentlich auf GitHub liegt. `fetchNaturaDb()` gibt jetzt hart `{}` zurück (`NATURADB_ENABLED = false`), unabhängig vom Settings-Toggle; Default-Quelle in `settings.ts` auf `enabled: false` gesetzt. **Möglichkeit für später:** aktiv Erlaubnis/API-Zugang bei NaturaDB einholen und `NATURADB_ENABLED` wieder auf `true` setzen — bis dahin bleibt die Quelle aus
- [x] **PFAF-Attribution auf den Karten ergänzt** — `hasSource(plant, 'pfaf')` (neu in `types.ts`) prüft, ob die Pflanze überhaupt PFAF-Felder enthält; nur dann erscheint „Daten: PFAF.org (CC BY 4.0)". Poly-Karte: Fußzeile unter den Blütemonaten. Streifenkarte: eigene feste Spalte am rechten Rand (nicht in der per Ellipsis gekürzten Namens-Zeile — dort wäre sie bei langen Pflanzennamen unsichtbar geworden, per Playwright-Test verifiziert). Baumscheibe: kleiner Text im freien Kuppel-Bereich unter dem Namen (`injectPfafAttribution()`). Getestet mit/ohne PFAF-Quelle auf allen drei Kartentypen plus Leerfall-Regression
- [x] **Bewusste Entscheidung: Browser-User-Agent statt selbst-identifizierendem** — PFAF filtert erkennbar auf den Outbound-User-Agent (siehe „Proxy: PFAF-Parsing" unten): ein ehrlicher, selbst benannter UA (`PermaGuildForge/1.0`) bekommt zuverlässig leere Daten, ein normaler Browser-UA funktioniert. Das ist eine andere Frage als die Datenlizenz (CC BY 4.0 erlaubt Weiterverwendung explizit) — UA-Filtering deutet eher darauf hin, dass *automatisierter* Zugriff unerwünscht ist, unabhängig von der Datennutzung selbst. Abgewogen und bewusst entschieden (im Gegensatz zu NaturaDB, wo mangels jeglicher Lizenz komplett deaktiviert wurde): mit Browser-UA weitermachen, da die Datennutzung selbst lizenzkonform ist und PFAFs eigener Lizenztext ungewöhnlich einladend zur Weiterverwendung ist. Siehe Kommentar bei `OUTBOUND_USER_AGENT` in `plant-proxy-server.mjs` für die volle Begründung — bei Bedarf (z.B. explizite PFAF-ToS zu automatisiertem Zugriff, oder deutlich wachsendes Anfragevolumen) neu bewerten
- [x] **Impressum-Lizenzangabe korrigiert** — `impressum.astro` nannte fälschlich „PFAF: CC BY-NC-SA 4.0"; korrigiert auf die tatsächliche Lizenz der Datenbank-Texte, CC BY 4.0 (NC-SA 3.0 gilt nur für Website-Bilder, die gar nicht kopiert werden)
- [x] **Offenes Proxy-Relay geschlossen** — *(Stand zum Zeitpunkt dieses Fixes, noch auf Netlify; siehe „Proxy von Netlify auf VPS migriert" weiter unten für den aktuellen Stand)*. `plant-proxy.mts` hatte `Access-Control-Allow-Origin: *` und kein Rate-Limiting; jede fremde Website konnte die Funktion als kostenlosen Scraping-Relay für PFAF zweckentfremden. Fix damals: Origin-Allowlist (`isAllowedOrigin()` — nur `*.netlify.app` inkl. Deploy-Previews, `localhost`/`127.0.0.1`, sowie Requests ganz ohne `Origin`-Header/server-to-server; alles andere bekommt `403`), plus von Netlify durchgesetztes Rate-Limiting über `config.rateLimit` (30 Requests/IP/60s). Mit esbuild kompiliert und die Origin-Logik gegen erlaubte/verbotene/fehlende Origins sowie OPTIONS-Preflight verifiziert

### Infrastruktur
- [x] **Proxy von Netlify auf eigenständigen Server migriert** — `pwa/netlify/functions/plant-proxy.mts` + `netlify.toml` entfernt, `@netlify/functions`-Dependency raus. Ersetzt durch `pwa/server/plant-proxy-server.mjs` (reines Node, keine Dependencies) als dauerhaft laufender Prozess (`plant-proxy.service` auf dem Entwicklungs-VPS). Frontend-Code unverändert (`fetch('/api/plant-proxy?...')`) — im Dev-Server übernimmt `vite.server.proxy` in `astro.config.mjs` die Weiterleitung an Port 8787 (`xfwd: true`, damit die echte Client-IP fürs Rate-Limiting ankommt); jedes andere Deployment braucht eine äquivalente Reverse-Proxy-Regel (z.B. nginx). CORS-Origin-Allowlist und Rate-Limiting (jetzt echtes In-Process-Limiting statt Netlifys `config.rateLimit`, da der Prozess dauerhaft läuft statt bei jedem Cold-Start zu resetten) portiert. Verifiziert: direkter Aufruf des Backends, Aufruf über den Dev-Server-Proxy, kompletter Such-→Wikidata-→Import-Flow per Headless-Browser. Dabei entdeckt (vorbestehend, keine Regression): PFAF hat sein HTML-Layout geändert, die Regex-Parser liefern fast nur noch leere Felder — siehe „Bekannte Probleme" unten
  - Datenschutzerklärung (`datenschutz.astro`) korrigiert: nannte noch „Netlify Serverless Function" und NaturaDB als aktive externe Quelle sowie Netlify als Hosting-Anbieter — alles nicht mehr zutreffend
  - Diverse Textstellen (`hilfe.astro`, `settings.astro`, `CLAUDE.md`, beide READMEs, `SETUP.md`) von „Netlify-Proxy"/„Netlify Function" auf „Server-Proxy" aktualisiert
- [x] **Frucht-/Blütemonate aus PFAF extrahiert** — PFAFs Fließtext-Beschreibung (`lblPhystatment`) enthält bei fast allen geprüften Pflanzen einen Satz wie „It is in flower from April to May … the seeds ripen from September to October" — exakte Monatsbereiche, kein Fließtext-Rätselraten nötig. (Es gibt daneben ein `txtSummary`-Feld mit „Main Bloom Time: Early spring, Late spring, Mid spring" — das ist aber nur Jahreszeit statt Monat und uneinheitlich formatiert, teils genereller Gattungs-Fließtext statt Blühzeit; bewusst nicht verwendet.) `parseMonthRange()`/`extractMonths()` in `plant-proxy-server.mjs` parsen den Satz und füllen `flowerMonths`/`fruitMonths`, inkl. korrektem Jahreswechsel bei Bereichen wie „November to March". Verifiziert an Fagus sylvatica (Apr–Mai / Sep–Okt), Robinia pseudoacacia (Jun / Nov–Mär, Jahreswechsel korrekt), Prunus avium, Corylus avellana

### UI / UX
- [x] **Freitext-Filter für die Tabelle** — neues Suchfeld in der Filter-Leiste (`#filter-text`), filtert live nach deutschem/lateinischem Namen (`.toLowerCase().includes()`, gleiche Konvention wie `searchDB()` in `plant-search.ts`), 150ms Debounce. Unabhängig von der oberen Suchleiste (die importiert neue Pflanzen aus externen Quellen). Kombiniert per AND mit den bestehenden Filter-Chips, geteilt über alle drei Ansichten (Kacheln/Liste/Karten) via `applyFilters()`, persistiert in localStorage (`plant-text-filter`) und wird von „Zurücksetzen"/Beispieldaten-Import mitzurückgesetzt.
- [x] **Ein sinnvoller Filter pro Tabellenspalte** — Nutzung/Funktionen als Mehrfachauswahl-Dropdown (`<details>`+Checkboxes, gleiches No-JS-Positionierungsmuster wie die Farblegende) statt der alten 4 Einzel-Chips (Essbar/Medizinisch/Material/N-Fixer), deckt jetzt alle 12 statt nur 4 Nutzung/Funktionen-Felder ab; ODER-Logik innerhalb einer Spalte (z.B. Essbar+Futter zeigt beides), UND-Logik über Spalten hinweg. Neu außerdem: Blüte und Frucht als zwei getrennte Monats-Mehrfachauswahl-Dropdowns (nicht ein kombiniertes Dropdown und nicht Einzelauswahl) — „blüht im Mai" und „fruchtet im September" sind unterschiedliche Fragen, ODER innerhalb von Blüte bzw. Frucht, UND zwischen beiden. H (Höhen-Mindestwert-Dropdown: ≥1/3/5/10/20m) und Vollst. (Mindest-Vollständigkeit: ≥25/50/75/100%, nutzt die bestehende `completenessPercent()`). Alte Chip-Filter (eatable/meds/material/nitrogenFix) werden beim Laden einmalig aus `plant-filters` in die neuen Dropdown-Filter migriert, statt beim Umstieg verloren zu gehen. Sonne-/Wasser-Chips bleiben unverändert (haben keine eigene Tabellenspalte). Alle neuen Filter persistiert, geteilt über alle drei Ansichten, in „Zurücksetzen" und Beispieldaten-Import-Reset eingebunden.

## Langfristig

### Mehrsprachigkeit
- [x] **UI auf Englisch lokalisieren** — Client-seitiges i18n (`src/lib/i18n/`), passend zur Architektur ohne Framework/Build-Routing: Sprachauswahl-Button oben rechts im Header (neben Theme-Toggle), Wahl wird in `localStorage` gespeichert (Default `de`), Umschalten lädt die Seite neu, damit alles in der gewählten Sprache neu rendert. `core.ts` stellt `getLang`/`setLang`, `createT(...dicts)` (gebundenes `t(key, vars?)` für JS-generierten Text) und `applyStaticI18n(...dicts)` (läuft über `data-i18n`/`-title`/`-placeholder`/`-aria-label`-Attribute im statischen Markup) bereit. Jede Seite hat ihr eigenes `dict-<seite>.ts`; `Layout.astro` übersetzt seinen eigenen Header/Footer/Banner/Shortcuts-Modal separat von den Einzelseiten. Wichtiger Bugfix dabei: `applyStaticI18n` durchsucht das *gesamte* Dokument, nicht nur das Markup des Aufrufers — ohne Anpassung hätte der Aufruf einer Seite mit ihrem eigenen Dict die bereits von `Layout.astro` übersetzten Nav-/Footer-Elemente mit dem rohen Key überschrieben (unbekannte Keys werden jetzt übersprungen statt mit dem Rohschlüssel überschrieben). Alle 8 Seiten vollständig übersetzt, inklusive Impressum/Datenschutz (mit Klarstellung: keine rechtsverbindliche Übersetzung, nur zur Verständlichkeit — deutsche §-Verweise wie TMG/MStV bleiben zitiert, mit englischer Erläuterung daneben). Per Playwright über alle 8 Routen in beiden Sprachen verifiziert (inkl. dynamisch gerenderter Inhalte: Tabellen-Header, Bulk-Leiste, Bearbeiten-Dialog, Druckanzahl-Stepper-Tooltips)

## Bekannte Probleme / Tech Debt (behoben)

| Problem | Status |
|---|---|
| PDF-Streifen in LibreWolf (SMask/pdf.js) | ✅ behoben — raw RGB via `flateStream` |
| Baumscheibe-PDF in Firefox | ✅ behoben — Firefox nutzt nativen Druckdialog (vektoriell) |
| Dark-Mode-Bulk-Substitution (View-Toggle-Buttons) | ✅ behoben |
| Beispieldaten-Import schlägt auf Deploy fehl | ✅ behoben — Filter-Reset + DB-v3-Upgrade |
| DB v2 fehlender `plants`-Store | ✅ behoben — v3 idempotente Upgrade-Logik |
| Proxy kein Rate-Limiting / offenes CORS | ✅ behoben — Origin-Allowlist + Rate-Limiting, siehe „Lizenz & Datenquellen" oben |
| Proxy lief über Netlify Function | ✅ behoben — auf eigenständigen Server migriert, siehe „Infrastruktur" oben |
| Proxy: PFAF-Parsing via Regex auf rohem HTML | ✅ Vollständig behoben und live verifiziert. Tabellenfelder (`commonName`, `eatableScore`, `medsScore`, `materialScore`, `climateZone`): PFAF hatte die Übersichtstabelle umgebaut (Label jetzt in `<b>`, Wert jetzt in `<span id="ContentPlaceHolder1_XXX">` statt direkt im `<td>`) — umgestellt auf Extraktion über die ASP.NET-Control-`id`s, stabiler als umgebendes Markup. Physical-Description-Block, Sonne/Wasser-Icons und Funktions-Tags waren nicht betroffen. **Der eigentliche Grund für die anfangs vermutete „Drosselung":** zwei sich überlagernde, unabhängige Bugs — (1) `plant-proxy-server.mjs` baute die PFAF-URL via `encodeURIComponent(name.replace(' ','+'))`, was das `+` selbst nochmal escapte zu `%2B` (→ PFAF suchte nach einer Pflanze namens „Malus+domestica" mit Plus-Zeichen im Namen, fand nichts, lieferte aber trotzdem `200 OK` mit leerer Seite statt eines Fehlers); Fix: erst `encodeURIComponent(name)`, dann `%20`→`+` ersetzen, nicht umgekehrt. (2) PFAF liefert dem User-Agent-String `PermaGuildForge/1.0` reproduzierbar (100% in Stichproben von je 5+ Anfragen) eine vollständig aufgebaute, aber komplett leere Seite — mit einem gewöhnlichen Browser-UA kommen dieselben Anfragen zuverlässig mit echten Daten zurück. Fix: `OUTBOUND_USER_AGENT` auf einen normalen Firefox-UA-String gesetzt. Mit `curl -sL` (Redirects folgen — wichtig, PFAF leitet `%20`-URLs per 302 auf die `+`-Form um) und direkten Node-`fetch()`-Vergleichen sauber isoliert und je einzeln bestätigt, dann beide Fixes zusammen live über Proxy und komplette App-UI (Suche → Wikidata → Import) verifiziert: Malus domestica liefert jetzt commonName „Apple", 9 m Höhe, Zone 3-8, Scores 5/2/4 etc.; Robinia pseudoacacia korrekt `nitrogenFix: true`, 25×15 m. NaturaDB-Parsing-Code existiert noch (`fetchNaturaDb()`), ist aber via `NATURADB_ENABLED = false` deaktiviert (siehe „Lizenz & Datenquellen" oben) |
| Backup-Restore ignorierte Polykulturen in allen pull/import-Pfaden | ✅ behoben — `importPolycultures()` (damals `importGuilds()`) in `db.ts`, alle vier Restore-Handler in `settings.astro` |
